
#include <stdio.h>
#include <math.h>

#define PI 3.14159265359

int k;
int limit;

for (k=0; k>=limit ; k++) {
		pow(2*PI*t, -0.5) * (	( ( ( y - x + 2ka) * ( y - x + 2ka) ) * -1 / 2 * t ) + 
											( ( ( y - x + 2ka) * ( y + x + 2ka) ) * -1 / 2 * t )  )

}

//where x and y are the values of the variables at the instants 0 and t, respectively, and k = 0, +/- 1, +/- 2, ...